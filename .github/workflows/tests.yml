---
name: Tests

on:
  push:
  workflow_dispatch:

jobs:
  tests:
    name: Unit Testing
    # commented out because switch to xUnit and a better UI ViewModel splitting
    if: ${{ false }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2.1.0
      - run: dotnet test